<% text = 'Insert New Article' if @article.new_record? %>
<% text = 'Edit Article' if !@article.new_record?%>

<%= simple_form_for @article do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.submit "#{text}"%>
<% end %>
